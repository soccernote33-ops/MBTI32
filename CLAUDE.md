# MBTI32

MBTI16タイプ×男女、計32キャラによる再現系ショート動画を作るプロジェクト。
「8種類のMBTIで恋愛リアリティーショーをやってみたら」のような企画を想定している。

素材（表情集・音声）はユーザーが用意し、このリポジトリのスクリプトが
そこから動画を組み立てる。

## データの置き場所

```
data/characters/       32キャラの氏名・読み・由来 (female.json / male.json)
                       voicevox_voice_map.json は初期検討時のもので、現在は未使用
data/scripts/          エピソードの台本。行ごとに話者・表情番号・セリフを持つ
data/assets/<キャラ>/  source/          表情集シートの原本 (2枚組)
                       expressions/     切り出した12表情
                       expressions_b/   口の形が違う方の12表情
                       mouth_frames/    口だけ差し替えた開閉2枚 × 12表情
                       mouth_pairs.json どの絵が開/閉かの対応表
data/episodes/<ep>/    voices/  台本の行番号とキャラ名で並べた音声 (01_yuina.wav)
```

`data/episodes/*/build/` と書き出した mp4 は再生成できるので追跡していない。

## 素材から動画までの手順

順番に依存関係があるので、この順で実行する。

```bash
python3 scripts/crop_expressions.py   # シート2枚を12パネルずつに切り出す
python3 scripts/align_variants.py     # 2枚の位置ずれを補正する
python3 scripts/pair_mouths.py        # どちらが口を開けた絵かを判定する
python3 scripts/isolate_mouth.py      # 口の部分だけ差し替えたコマを作る
python3 scripts/build_episode.py --script data/scripts/ep01_chikoku.json \
                                 --voice-dir data/episodes/ep01/voices
```

口パクだけを顔アップで確認したいときは `scripts/lipsync_preview.py`。
しきい値を変えて見比べられる。

## 判断の経緯（同じ回り道をしないために）

**声**: 実在の声優の声を無断で模倣する案は、パブリシティ権と契約の問題で不採用。
無料かつ商用利用可能な COEIROINK を使っている。声の選定はキャラへの愛着を
左右するため、ユーザーが実際に聴いて選ぶ。

**表情集のラベル**: シートに焼き込まれた表情名は動画に映り込む。絵に重なって
いる場合、インペイントで消そうとすると髪ごと溶けて失敗する。ユーザーに
ラベルなしで生成し直してもらうのが唯一きれいな解決だった。

**口パク**: 表情集を2枚用意し、音量に応じて開閉を切り替える。ただし2枚は
別々にAI生成されているため、口以外（髪・輪郭・顎）も違う。そのまま交互に
出すと画面全体がチラついて口パクに見えないので、`isolate_mouth.py` で
開いた絵を土台に固定し、口のまわりだけを貼り替えている。

**口の位置**: `isolate_mouth.py` の `MOUTH_BOXES` はキャラごとの実測値。
差分のピーク・口内の色・両者の積・強いぼかし、の4通りを試したが、髪の
描き分けや目の影に引っ張られてどれも安定しなかった。**シートを作り直したら
目視で取り直すこと。**

**顎の歪み**: 貼り替え範囲に顎が入ると、形が2枚で違うぶん口が動くたびに
輪郭が飛ぶ。範囲を口元に絞り、貼る前に周囲が重なる位置へずらして抑えている。
根本的には、2枚目を「1枚目から口だけ描き替える」形（同じシード値/img2img）で
生成できれば解決する。

## 現状

- EP01『遅刻はどこまで許せるか』は音声20本すべて収録済み、動画も書き出し済み
- 素材が揃っているのは4キャラ（花森結菜・水城凪沙・藤宮理玖・金城瞬）のみ
- リップシンクの品質を1キャラずつ詰めている段階
- 残り28キャラは未着手。シートが届けば同じ手順で処理できる
